import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btnContainer: {
    padding: 10,
    borderRadius: 10,
    flexDirection: "row",
    alignItems: "center",
    width: 170,
    height:35
  },
  btnText: {
    fontSize: 16,
    textAlign: 'center',
    flex: 1,
    whiteSpace: 'nowrap',
  },
});

export default styles;
