import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btnContainer: {
    padding: 10,
    borderRadius: 5,
    flexDirection: "row",
    alignItems: "center",
    width: 170,
  },
  btnText: {
    fontWeight: "bold",
    fontSize: 16,   
    textAlign: 'center',
    flex: 1,
  },
});

export default styles;
